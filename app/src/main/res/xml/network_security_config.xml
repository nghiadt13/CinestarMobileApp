<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cho phép HTTPS mặc định và tin cậy các certificate hệ thống -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Cho phép HTTP chỉ cho các domain cụ thể -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Ngrok domain (sẽ thay đổi mỗi lần) -->
        <domain includeSubdomains="true">ngrok-free.app</domain>
        <domain includeSubdomains="true">ngrok.io</domain>
        <domain includeSubdomains="true">ngrok-free.dev</domain>
        <!-- Localhost & IP (khi test local) -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain> <!-- Android Emulator -->
    </domain-config>

    <!-- Cấu hình riêng cho YouTube để đảm bảo HTTPS hoạt động -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">youtube.com</domain>
        <domain includeSubdomains="true">youtu.be</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">ytimg.com</domain>
        <domain includeSubdomains="true">googlevideo.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>